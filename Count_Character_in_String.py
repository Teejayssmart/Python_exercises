# Implement a function count_letter that takes two parameters :
# a string and a letter, then returns the  number of times the
# letter  appears in a string
#

def count_letter(word, letter):
    index = 0  # to count and hold each loop
    counter = 0  # to count & hold number of times a letter matches
    while index < len(word):
        word[index]
        if letter in word[index]:
            counter = + 1
        index = + 1

